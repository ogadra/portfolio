---
import type { GithubStats } from '../lib/github';

interface Props {
	stats: GithubStats | null;
}

const { stats } = Astro.props;

// The ticker reads as instrument text rather than prose, so it stays in the one
// form the HUD is drawn around and never goes through the message catalogue.
const message = [
	'GITHUB github.com/ogadra',
	'TWITTER @const_myself',
	`PUBLIC REPOS ${stats?.publicRepos ?? '--'}`,
	'STACK TypeScript / Go / HCL',
	'CLASSIFICATION SOFTWARE ENGINEER',
	'SITE ogadra.com / ASTRO ON CLOUDFLARE WORKERS',
].join('  ///  ');
---

<footer class="ticker" aria-hidden="true">
	<div class="hazard"></div>
	<div class="track">
		<span>{message}&nbsp;&nbsp;///&nbsp;&nbsp;</span>
		<span>{message}&nbsp;&nbsp;///&nbsp;&nbsp;</span>
	</div>
</footer>

<style>
	@import './StatusTicker.css';
</style>
